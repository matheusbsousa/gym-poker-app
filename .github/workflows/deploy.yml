name: Initial flow
on:
  workflow_dispatch:
  push:
    branches:
      - main
jobs:
  build:
    name: Build application
    runs-on: ubuntu-latest
    steps:
      - name: Test step
        run: echo "Hello world"